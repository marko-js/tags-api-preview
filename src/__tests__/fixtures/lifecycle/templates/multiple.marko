<props/{ onCount, onMount, onDestroy }/>
<let/count=0/>

<button onClick=(() => count++)>increment</button>

<lifecycle
  onMount=onMount
  onDestroy=onDestroy
  onUpdate=(() => onCount(count))
/>

<lifecycle
  onMount=onMount
  onDestroy=onDestroy
  onUpdate=(() => onCount(count))
/>
