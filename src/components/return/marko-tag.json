{
  "translate": "./translate",
  "@value": {
    "type": "expression",
    "required": true,
    "autocomplete": [
      {
        "description": "Data that will be provided as a tag variable to the parent tag."
      }
    ]
  },
  "@valueChange": {
    "type": "expression",
    "autocomplete": [
      {
        "description": "A function that is called whenever the parent tag assigns to the base tag variable."
      }
    ]
  },
  "autocomplete": [
    {
      "displayText": "return=<data>",
      "description": "A tag that allows providing a tag variable to the parent tag.",
      "snippet": "return=$1"
    }
  ]
}
